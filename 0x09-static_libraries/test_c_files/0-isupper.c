#include "main.h"

/**
 * _isupper - checks for uppercase character
 * @ben: the character to be checked
 * Return: 1 if c is uppercase, 0 otherwise
 */
int _isupper(int ben)
{
	return (ben >= 'A' && ben <= 'Z');
}
