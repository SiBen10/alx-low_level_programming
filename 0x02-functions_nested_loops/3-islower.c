#include "main.h"

/**
 * _islower - checks for lowercase character
 * @f: the character to check
 * Return: 1 if f is lowercase, 0 otherwise
 */
int _islower(int f)
{
	return (f >= 'a' && f <= 'z');
}
